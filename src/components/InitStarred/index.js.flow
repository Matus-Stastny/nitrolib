// @flow strict
import * as React from "react";

import type { StarredItem } from "../../records/Starred";

type Args = {|
  starredList: StarredItem[],
  onRemove: (arg: number, e: SyntheticMouseEvent<HTMLDivElement>) => void,
  onAdd: (arg: StarredItem) => void,
  lang: string,
  onClear: (e: SyntheticMouseEvent<HTMLDivElement>) => void,
|};

type Props = {|
  children: (args: Args) => React.Node,
|};

declare export default React.ComponentType<Props>;
